{
  "name": "@transmiss/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -b --pretty"
  },
  "dependencies": {
    "@transmiss/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250610.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.20.5"
  }
}
